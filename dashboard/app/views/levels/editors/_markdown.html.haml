:scss
  $cyan: #0094ca;
  $light_cyan: #59b9dc;
  $lighter_cyan: #a6daed;
  $lightest_cyan: #d9eff7;

  .script_text textarea { width: 100%; height: 100px}
  .markdown {
    border: 5px solid gray;
    border-radius: 5px;
    padding: 10px;
  }

  .markdown.teacher {
    border: 5px solid $cyan;
    background-color: $lightest_cyan;
  }

- markdown_data = {dsl: local_assigns[:dsl]}
- teacher_only = local_assigns[:teacher_only]

- if @level.supports_markdown?
  - content_for(:head) do
    /TODO: update name
    %script{src: minifiable_asset_path('js/levelbuilder_markdown.js'), data: {markdown: markdown_data.to_json}}

  - unless teacher_only
    .markdown
      %p Edit Markdown:

      %textarea#level_markdown_textarea
      #level_markdown_textarea_preview

  .markdown.teacher
    %p Edit Teacher Only Markdown:

    %textarea#level_teacher_markdown_textarea
    #level_teacher_markdown_textarea_preview
